---
phase: 07-config-models-setup
plan: 03
type: execute
---

<objective>
Build frontend UI for managing AI models: Config menu entry, Models page,
form components, and integration with existing persona creation.

Purpose: Provide user-friendly interface for defining and testing AI models,
replacing manual editing of models.json.

Output: New React components: ConfigMenu, ModelsPage, ModelForm, ModelCard,
integrated with TanStack Query hooks. Sidebar updated with Config link.
</objective>

<context>
@.planning/pcw-101-ai-dashboard-core-refactor/BRIEF.md
@.planning/pcw-101-ai-dashboard-core-refactor/ROADMAP.md
@packages/client/src/layouts/Sidebar.tsx
@packages/client/src/pages/PersonasPage.tsx (reference)
@packages/client/src/components/ui (existing component library)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API hooks for models</name>
  <files>
    packages/client/src/api/models.ts
  </files>
  <action>
    Create TanStack Query hooks for models CRUD operations:
    - `useModels()` (list)
    - `useModel(id)`
    - `useCreateModel()`
    - `useUpdateModel()`
    - `useDeleteModel()`
    - `useTestModel()`

    Follow pattern from existing hooks (e.g., `packages/client/src/api/personas.ts`).

    Ensure proper TypeScript types (import from server types or define locally).
  </action>
  <verify>
    `pnpm --filter client build` passes.
    Hooks compile.
  </verify>
  <done>
    API hooks ready.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Config menu to sidebar</name>
  <files>
    packages/client/src/layouts/Sidebar.tsx
  </files>
  <action>
    Update Sidebar component: add a new menu item "Config" (icon: Settings) between
    "Projects" and "Monitor" (or after "Personas").

    Link to `/config` route.

    Follow existing styling (Tailwind classes).
  </action>
  <verify>
    Sidebar shows Config link.
    `pnpm --filter client build` passes.
  </verify>
  <done>
    Sidebar updated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create ModelsPage and ModelForm components</name>
  <files>
    packages/client/src/pages/ConfigPage.tsx
    packages/client/src/components/models/ModelForm.tsx
    packages/client/src/components/models/ModelCard.tsx
  </files>
  <action>
    Create `ConfigPage` as layout that may have tabs (Models, Settings). For now,
    just Models tab.

    Create `ModelForm` component for create/edit with fields:
    - Name (text)
    - Provider (dropdown: Anthropic, DeepSeek, OpenAI, Z.AI, etc.)
    - Model ID (text input with suggestions based on provider)
    - API Key (password input, optional)
    - Config JSON (textarea, optional)

    Use React Hook Form + Zod validation.

    Create `ModelCard` component to display each model with actions (Edit, Delete, Test).

    Use existing UI components (Button, Input, Card, etc.) from `@/components/ui`.

    Integrate with the API hooks.

    Add "Test Connection" button that calls test endpoint and shows result.
  </action>
  <verify>
    Page renders without errors.
    Form validation works.
    CRUD operations functional.
  </verify>
  <done>
    Models page fully functional.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update persona creation to use models dropdown</name>
  <files>
    packages/client/src/components/personas/PersonaForm.tsx
  </files>
  <action>
    Enhance PersonaForm: when selecting model, provide a dropdown of available
    models from the database (via `useModels()`). Allow custom input as fallback.

    Display provider and model ID together.

    Ensure backward compatibility: if no models in DB, show free text input.
  </action>
  <verify>
    Persona form works with both dropdown and manual input.
  </verify>
  <done>
    Persona creation improved.
  </done>
</task>

</tasks>

<verification>
- [ ] Config menu appears in sidebar
- [ ] Models page loads and displays models
- [ ] Create, edit, delete, test operations work
- [ ] Persona form can select from models dropdown
- [ ] `pnpm --filter client build` passes
</verification>

<success_criteria>
- Config UI fully functional
- Users can manage AI models via web interface
- Persona creation integrates with model list
- Ready for final integration (07-04)
</success_criteria>

<output>
After completion, create SUMMARY.md.
</output>