---
phase: 07-config-models-setup
plan: 02
type: execute
---

<objective>
Implement server routes for model CRUD operations, validation, and test endpoint.
Enable full backend API for managing AI models via the future Config UI.

Purpose: Provide REST API for the frontend to create, read, update, delete,
and test AI model configurations.

Output: Complete routes in `packages/server/src/routes/models.ts` with Zod validation,
error handling, and test endpoint that verifies model connectivity.
</objective>

<context>
@.planning/pcw-101-ai-dashboard-core-refactor/BRIEF.md
@.planning/pcw-101-ai-dashboard-core-refactor/ROADMAP.md
@packages/server/src/services/modelService.ts
@packages/server/src/routes/models.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance models route with CRUD endpoints</name>
  <files>
    packages/server/src/routes/models.ts
  </files>
  <action>
    Update existing `models.ts` route file (currently only GET /api/models).
    Add new endpoints:
    - POST /api/models (create)
    - PUT /api/models/:id (update)
    - DELETE /api/models/:id (soft delete)
    - GET /api/models/:id (get single)
    - GET /api/models/:id/test (test connectivity)

    Use Zod schemas for validation of input fields (name, provider, modelId, apiKey optional, config JSON).
    Ensure proper error responses (400, 404, 500).

    Integrate with `modelService` created in 07-01.

    **Test endpoint**: Use OpenCode SDK or provider-specific SDK to test API key
    (if provided) with a simple ping. For now, simulate with timeout; later implement
    actual provider SDK calls.

    **Authentication**: Protect routes with existing bearer token middleware.
  </action>
  <verify>
    `pnpm --filter server build` passes.
    Endpoints respond correctly (test via curl).
  </verify>
  <done>
    CRUD routes functional.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add provider-specific validation and model normalization</name>
  <files>
    packages/server/src/services/modelService.ts
    packages/server/src/utils/modelUtils.ts (new)
  </files>
  <action>
    Create `modelUtils.ts` with helper functions:
    - `normalizeModel` (import from planningRunner)
    - `validateProvider(provider)` returns boolean
    - `parseModelString(fullString)` splits provider/modelId
    - `getProviderEnvVar(provider)` returns expected env var name

    Add validation in `modelService.createModel` and `updateModel`:
    - Provider must be known (allow list)
    - Model ID must be valid (no slashes)
    - If apiKey provided, store; else warn that environment variable must be set.

    **Environment variable fallback**: When using a model, the runner should check
    first for apiKey in database, then fall back to env var (e.g., ANTHROPIC_API_KEY).
  </action>
  <verify>
    Validation rejects invalid provider/modelId.
    `pnpm --filter server build` passes.
  </verify>
  <done>
    Validation utilities added.
  </done>
</task>

</tasks>

<verification>
- [ ] All endpoints return appropriate HTTP status codes
- [ ] Validation works for invalid inputs
- [ ] Test endpoint returns meaningful response (success/failure)
- [ ] No breaking changes to existing GET /api/models
</verification>

<success_criteria>
- CRUD API fully functional
- Validation ensures data integrity
- Test endpoint provides basic connectivity check
- Ready for frontend integration (07-03)
</success_criteria>

<output>
After completion, create SUMMARY.md.
</output>