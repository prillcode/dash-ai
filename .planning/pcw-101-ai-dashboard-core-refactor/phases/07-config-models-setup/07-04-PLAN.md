---
phase: 07-config-models-setup
plan: 04
type: execute
---

<objective>
Integrate models table with existing systems: migrate models.json data,
update runners to prefer database models, and ensure backward compatibility.

Purpose: Complete the transition from file-based models.json to database,
making the Config feature fully operational.

Output: Seamless integration, data migration script, updated runners,
and final verification.
</objective>

<context>
@.planning/pcw-101-ai-dashboard-core-refactor/BRIEF.md
@.planning/pcw-101-ai-dashboard-core-refactor/ROADMAP.md
@packages/server/src/db/client.ts
@packages/server/src/opencode/planningRunner.ts
@packages/server/src/opencode/codingRunner.ts
@~/.ai-dashboard/models.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration script for models.json</name>
  <files>
    packages/server/src/scripts/migrateModelsJson.ts
  </files>
  <action>
    Create a one‑time migration script that reads `~/.ai-dashboard/models.json`
    and inserts each model into the database (if not already present).

    Handle duplicates by name or provider+modelId.

    Script should be idempotent: run at server startup (optional) or via CLI command.

    Add to `package.json` scripts: `"migrate:models": "tsx packages/server/src/scripts/migrateModelsJson.ts"`

    **Fallback**: Keep models.json as backup; after migration, the GET /api/models
    endpoint should prioritize database, then fall back to file (if table empty).
  </action>
  <verify>
    Script runs without error, inserts data.
    Existing models still accessible via API.
  </verify>
  <done>
    Migration script ready.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update runners to use database models</name>
  <files>
    packages/server/src/opencode/planningRunner.ts
    packages/server/src/opencode/codingRunner.ts
    packages/server/src/services/modelService.ts
  </files>
  <action>
    Modify `planningRunner` and `codingRunner` to optionally fetch model config
    from database (by provider/modelId) and use stored API key if present.

    If apiKey in database, set environment variable temporarily for the session
    (or pass to SDK if supported). Otherwise rely on existing env vars.

    No breaking changes: if model not found in DB, use current behavior.

    **Note**: OpenCode SDK reads env vars automatically; we can set `process.env.ANTHROPIC_API_KEY`
    dynamically (but careful with concurrency). Safer to rely on env vars set by user.
    Store API key in database for reference only, not for runtime use (for now).

    Update error messages to guide users to Config page if model configuration missing.
  </action>
  <verify>
    Runners still work with existing env vars.
    `pnpm --filter server build` passes.
  </verify>
  <done>
    Runners aware of database models.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update AGENTS.md and README</name>
  <files>
    AGENTS.md
    README.md
  </files>
  <action>
    Update AGENTS.md with new conventions for model configuration (database vs file).
    Add note about Config menu and models table.

    Update README.md section "Setting up AI Models" to describe the new UI approach,
    with fallback to models.json for advanced users.

    Ensure documentation reflects the additive migration rule.
  </action>
  <verify>
    Documentation changes are clear.
  </verify>
  <done>
    Docs updated.
  </done>
</task>

<task type="auto">
  <name>Task 4: Final integration test</name>
  <files>
    (test manually)
  </files>
  <action>
    Perform end‑to‑end test:
    1. Start server, ensure models table exists.
    2. Open Config page, create a new model (provider: anthropic, model: claude‑sonnet‑4.6, no API key).
    3. Create a persona using that model.
    4. Create a task with planning persona, start planning (should work with existing env var).
    5. Verify models.json still works if table empty.

    Check that all previous functionality remains intact.
  </action>
  <verify>
    All steps succeed.
    No regression errors.
  </verify>
  <done>
    Integration validated.
  </done>
</task>

</tasks>

<verification>
- [ ] Migration script runs without data loss
- [ ] Runners function with both database and file models
- [ ] Config UI works end‑to‑end
- [ ] Documentation updated
- [ ] `pnpm build` passes for both client and server
</verification>

<success_criteria>
- Models.json data migrated to database
- Runners can utilize database model configurations
- Config feature complete and usable
- Backward compatibility maintained
- Phase 07 ready for production
</success_criteria>

<output>
After completion, create SUMMARY.md for the whole phase.
</output>