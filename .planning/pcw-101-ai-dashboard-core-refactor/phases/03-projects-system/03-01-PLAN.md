---
phase: 03-projects-system
plan: 01
type: execute
---

<objective>
Add provider/model awareness to Personas: a `provider` column in the DB, a user-editable
`~/.ai-dashboard/models.json` config, a `GET /api/models` endpoint that serves it, and
an updated `PersonaForm` with provider → model cascading dropdowns.

Purpose: Lets users assign high-reasoning models (Opus, o3) to planning personas and
efficient models (Sonnet, local Ollama) to coding personas, matching whatever their
OpenCode installation supports.

Output: `provider` column on personas table (migration applied), default `models.json`
shipped to `~/.ai-dashboard/`, new `/api/models` route, updated `PersonaForm` UI,
`pnpm --filter server build` clean.
</objective>

<execution_context>
@~/.agents/skills/create-plans/workflows/execute-phase.md
@~/.agents/skills/create-plans/templates/summary.md
@~/.agents/skills/create-plans/references/checkpoints.md
</execution_context>

<context>
@.planning/pcw-101-ai-dashboard-core-refactor/BRIEF.md
@.planning/pcw-101-ai-dashboard-core-refactor/ROADMAP.md
@.planning/pcw-101-ai-dashboard-core-refactor/phases/02-task-state-machine/02-03-SUMMARY.md
@packages/server/src/db/schema.ts
@packages/server/src/db/migrations/0000_thin_triathlon.sql
@packages/server/src/db/migrations/meta/0000_snapshot.json
@packages/server/src/services/personaService.ts
@packages/server/src/index.ts
@packages/client/src/components/personas/PersonaForm.tsx
@packages/client/src/types/persona.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `provider` column to personas schema + migration</name>
  <files>
    packages/server/src/db/schema.ts,
    packages/server/src/db/migrations/0000_thin_triathlon.sql,
    packages/server/src/db/migrations/meta/0000_snapshot.json
  </files>
  <action>
    In `schema.ts`, add `provider` text field to the `personas` table after `model`:
    `provider: text("provider").notNull().default("anthropic")`

    Because drizzle-kit generate prompts interactively (non-TTY issue documented in 02-01-SUMMARY),
    update the migration SQL and snapshot directly — do NOT run `pnpm db:generate`.

    In `0000_thin_triathlon.sql`, add the column to the CREATE TABLE `personas` block:
    `\`provider\` text DEFAULT 'anthropic' NOT NULL,`
    Place it immediately after the `model` line.

    In `0000_snapshot.json`, add the `provider` column entry to `personas.columns`:
    ```json
    "provider": {
      "name": "provider",
      "type": "text",
      "primaryKey": false,
      "notNull": true,
      "autoincrement": false,
      "default": "'anthropic'"
    }
    ```

    Then wipe the existing DB and re-apply the migration:
    ```bash
    export NVM_DIR="$HOME/.nvm" && source "$NVM_DIR/nvm.sh" && nvm use v22
    rm -f ~/.ai-dashboard/dashboard.db ~/.ai-dashboard/dashboard.db-wal ~/.ai-dashboard/dashboard.db-shm
    cd packages/server && pnpm db:migrate
    ```
  </action>
  <verify>
    `pnpm db:migrate` exits 0 and prints "migrations applied successfully".
    `sqlite3 ~/.ai-dashboard/dashboard.db ".schema personas"` shows `provider` column.
  </verify>
  <done>
    `provider` column exists on personas table with default 'anthropic'.
    Migration applied cleanly. Schema snapshot updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ship default models.json + GET /api/models route</name>
  <files>
    packages/server/src/db/client.ts,
    packages/server/src/routes/models.ts,
    packages/server/src/index.ts
  </files>
  <action>
    **Default models.json:**
    On server startup (in `db/client.ts` where the DB dir is already created), also write
    `~/.ai-dashboard/models.json` if it does not already exist. Use `existsSync` + `writeFileSync`.

    Default content:
    ```json
    {
      "providers": [
        {
          "id": "anthropic",
          "name": "Anthropic",
          "models": [
            { "id": "claude-opus-4-5", "name": "Claude Opus 4.5", "note": "best for planning" },
            { "id": "claude-sonnet-4-5", "name": "Claude Sonnet 4.5", "note": "best for coding" },
            { "id": "claude-haiku-3-5", "name": "Claude Haiku 3.5", "note": "fast + cheap" }
          ]
        },
        {
          "id": "openai",
          "name": "OpenAI",
          "models": [
            { "id": "o3", "name": "o3", "note": "best for planning" },
            { "id": "gpt-4o", "name": "GPT-4o", "note": "fast + capable" }
          ]
        },
        {
          "id": "ollama",
          "name": "Ollama (local)",
          "models": [
            { "id": "qwen2.5-coder:32b", "name": "Qwen 2.5 Coder 32B", "note": "strong local coding model" },
            { "id": "llama3.3:70b", "name": "Llama 3.3 70B", "note": "strong local general model" }
          ]
        }
      ]
    }
    ```

    **GET /api/models route** (`packages/server/src/routes/models.ts`):
    - Read `~/.ai-dashboard/models.json` (resolve `~` via `os.homedir()`)
    - Return parsed JSON
    - If file missing, return the hardcoded default above (fallback)
    - No auth required — this is read-only config

    Wire into `index.ts`:
    ```typescript
    import { modelsRouter } from "./routes/models"
    // ...
    app.route("/api/models", modelsRouter)
    ```
    Place BEFORE the `app.use("/api/*", authMiddleware)` line so it's public.
  </action>
  <verify>
    After `pnpm dev` (or after build): `curl http://localhost:3000/api/models` returns JSON
    with `providers` array containing anthropic, openai, ollama entries.
    `~/.ai-dashboard/models.json` exists on disk.
  </verify>
  <done>
    `GET /api/models` returns provider/model config from disk (or fallback).
    File auto-created on first server start if missing.
    Route is public (no auth token required).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update PersonaService + PersonaForm for provider field</name>
  <files>
    packages/server/src/services/personaService.ts,
    packages/client/src/types/persona.ts,
    packages/client/src/components/personas/PersonaForm.tsx,
    packages/client/src/api/personas.ts
  </files>
  <action>
    **Server — `personaService.ts`:**
    - Add `provider: string` to `ParsedPersona` interface (after `model`)
    - Add `provider?: string` to `PersonaInput` interface
    - `parsePersona`: include `provider` (already in `...row` spread, just ensure interface declares it)
    - `createPersona`: default `provider` to `"anthropic"` if not provided
    - `updatePersona`: allow `provider` to be updated (it passes through `...input` already, but ensure the Partial allows it)

    **Client — `types/persona.ts`:**
    - Add `provider: string` to `Persona` interface
    - Add `provider?: string` to `PersonaInput` interface

    **Client — `api/personas.ts`:**
    - Add `useModels()` hook: `useQuery` against `GET /api/models`, returns the providers array.
      No auth header needed (public route). `staleTime: Infinity` (config rarely changes).

    **Client — `PersonaForm.tsx`:**
    Replace the free-text "Model" `FormField` with a cascading provider → model picker:
    - Call `useModels()` to get provider list
    - Provider `<select>` (controlled): default to `initialData?.provider ?? "anthropic"`
    - Model `<select>` (controlled): options filtered to selected provider's models
    - When provider changes, reset model to first model of new provider
    - Display each model option as `{model.name}` with `{model.note}` in parentheses if present
    - Use `setValue("provider", ...)` and `setValue("model", ...)` from react-hook-form
    - Add `provider` to the Zod `personaSchema` (optional string, default "anthropic")

    Do NOT use a free-text input for model anymore — the dropdown enforces valid values
    from the config. If `useModels()` is loading, show a disabled select with "Loading...".
  </action>
  <verify>
    `pnpm --filter server build` exits 0.
    `pnpm --filter client build` exits 0.
  </verify>
  <done>
    `provider` field flows end-to-end: DB → service → API → client types → PersonaForm.
    PersonaForm shows cascading provider/model dropdowns populated from `/api/models`.
    Both server and client build clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Provider/model awareness on personas:
    - `provider` column in DB (default 'anthropic')
    - `~/.ai-dashboard/models.json` ships with Anthropic, OpenAI, Ollama presets
    - `GET /api/models` (public) returns config
    - PersonaForm now has cascading Provider → Model dropdowns instead of free-text Model field
  </what-built>
  <how-to-verify>
    1. Run: `export NVM_DIR="$HOME/.nvm" && source "$NVM_DIR/nvm.sh" && nvm use v22 && pnpm dev`
    2. Visit: http://localhost:5173/personas/new (or http://localhost:3000/personas/new if built)
    3. Confirm: Provider dropdown shows Anthropic / OpenAI / Ollama
    4. Confirm: Switching provider updates the Model dropdown options
    5. Create a test persona with provider=OpenAI, model=o3 — save and verify it persists on reload
    6. Run: `curl http://localhost:3000/api/models` (no token) — confirm returns JSON without 401
  </how-to-verify>
  <resume-signal>Type "approved" to continue to 03-02, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --filter server build` exits 0
- [ ] `pnpm --filter client build` exits 0
- [ ] `provider` column exists in DB schema
- [ ] `~/.ai-dashboard/models.json` auto-created on server start
- [ ] `GET /api/models` returns providers array without auth
- [ ] PersonaForm cascading dropdowns work
- [ ] Human checkpoint approved
</verification>

<success_criteria>
- All tasks completed
- Both builds pass clean
- Provider/model picker works end-to-end in PersonaForm
- Human has verified UI and API behavior
</success_criteria>

<output>
After completion, create `.planning/pcw-101-ai-dashboard-core-refactor/phases/03-projects-system/03-01-SUMMARY.md`:

# Phase 03 Plan 01: Persona Provider/Model — Summary

**[Substantive one-liner]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- [Files with descriptions]

## Decisions Made
- [Key decisions]

## Issues Encountered
- [Problems and resolutions, or "None"]

## Next Step
Ready for 03-02-PLAN.md (Projects schema + service + routes).
</output>
