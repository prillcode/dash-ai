---
phase: 06-setup-portability
plan: 01
type: execute
---

<objective>
Rewrite README.md to document the full planning-first workflow, Projects setup,
skill stack prerequisites, SQLite setup, and PM2 deployment. Update `.env.example`
to match the current SQLite-only stack. Update `AGENTS.md` to reflect current
conventions. Write a brief first-run setup section.

Purpose: The tool is useless without clear setup docs. This is the portability
gate — without this, a fresh clone won't get far. Also required for open-sourcing.

Output: `README.md` rewritten end-to-end; `.env.example` cleaned up; `AGENTS.md`
updated to reflect post-refactor conventions.
</objective>

<execution_context>
@~/.agents/skills/create-plans/workflows/execute-phase.md
@~/.agents/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/pcw-101-ai-dashboard-core-refactor/BRIEF.md
@.planning/pcw-101-ai-dashboard-core-refactor/ROADMAP.md
@.planning/pcw-101-ai-dashboard-core-refactor/phases/05-planning-ui/05-03-SUMMARY.md
@README.md
@.env.example
@AGENTS.md
@packages/server/src/db/client.ts
@packages/server/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md</name>
  <files>
    README.md
  </files>
  <action>
    Read the existing `README.md` first, then rewrite it completely.

    The new README must cover:

    ## Structure

    ```markdown
    # AI Dashboard

    A self-hosted, planning-first AI kanban board for managing coding tasks across local repos.

    ## How it works
    [Brief workflow description: Projects → Tasks → Planning Phase → Coding Phase → Review]

    ## Prerequisites
    - Node.js 22+ (via nvm recommended)
    - pnpm
    - OpenCode installed globally (for agent execution): see opencode.ai for install instructions
    - Planning skill stack (required for Planning mode):
      - `npx @prillcode/start-work` — scaffolds planning directories
      - Skills auto-installed at `~/.agents/skills/`

    ## Quick Start

    ### 1. Clone and install
    git clone ...
    pnpm install

    ### 2. Configure environment
    cp .env.example .env
    # Edit .env: set API_TOKEN to a secret string

    ### 3. Set up your AI provider
    # On first run, ~/.ai-dashboard/models.json is auto-created.
    # Edit it to add your Anthropic API key and preferred models.
    # Default location: ~/.ai-dashboard/models.json

    ### 4. Apply database migrations
    pnpm db:migrate
    # DB auto-created at ~/.ai-dashboard/dashboard.db

    ### 5. Run in development
    pnpm dev
    # Server: http://localhost:3000
    # Client: http://localhost:5173

    ## Planning Workflow

    [Explain: Create Project → Create Task with Planning Persona → Start Planning →
    AI runs start-work + create-plans in the repo → Review BRIEF.md + ROADMAP.md
    in the dashboard → Mark Ready to Code → Coding starts automatically]

    ## Projects

    [Explain: register local repos by name + path (supports ~), path validated on save]

    ## Personas

    [Explain: Planner persona (no bash tool, opus model) vs Coder persona (full tools, sonnet model);
    custom personas for flexibility; presets auto-populate fields]

    ## Deployment (PM2)

    [Basic PM2 steps for persistent local running]

    ## Environment Variables

    | Variable | Description | Default |
    |----------|-------------|---------|
    | API_TOKEN | Bearer token for auth | required |
    | PORT | Server port | 3000 |
    | SQLITE_DB_PATH | DB file path | ~/.ai-dashboard/dashboard.db |

    ## Database

    SQLite local file at `~/.ai-dashboard/dashboard.db`.
    Never use `pnpm db:generate` — always write migration SQL manually (see AGENTS.md).

    ## Tech Stack

    [Table: Node.js, Hono, better-sqlite3, Drizzle ORM, React 18, Vite, Tailwind CSS v3,
    TanStack Query v5, React Hook Form + Zod, React Router v6, OpenCode SDK]
    ```

    Keep the README concise — aim for under 200 lines. No corporate filler.
    Use real commands that actually work (verify them against the codebase).
  </action>
  <verify>
    `README.md` exists and is not empty.
    All commands in Quick Start section are accurate (verify against actual package.json scripts).
    No references to Turso or libSQL.
  </verify>
  <done>
    README.md completely rewritten.
    All commands verified against actual project structure.
    Zero Turso/libSQL references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .env.example + AGENTS.md</name>
  <files>
    .env.example,
    AGENTS.md
  </files>
  <action>
    Read the current `.env.example` and `AGENTS.md` before editing.

    **`.env.example` rewrite:**
    Must contain ONLY the variables currently used by the server.
    Check `packages/server/src/env.ts` and `packages/server/src/index.ts` to confirm
    which env vars are actually read. Remove any Turso/libSQL vars.

    Expected content:
    ```
    # AI Dashboard — Environment Variables
    # Copy to .env and fill in values

    # Required: Bearer token for API auth
    # Generate with: openssl rand -hex 32
    API_TOKEN=your-secret-token-here

    # Server port (optional, default: 3000)
    PORT=3000

    # SQLite database path (optional, default: ~/.ai-dashboard/dashboard.db)
    # SQLITE_DB_PATH=/custom/path/dashboard.db

    # Max concurrent AI sessions (optional, default: 3)
    # MAX_CONCURRENT_SESSIONS=3
    ```

    Also update `packages/client/.env.example` (or `packages/client/.env`) if it
    still references Turso vars or old API URL patterns. The client typically only
    needs `VITE_API_TOKEN`.

    **`AGENTS.md` updates:**
    Read the current file. Make targeted updates to reflect post-refactor state:

    1. **Tech Stack table** — verify it's accurate (SQLite, not Turso; OpenCode SDK)
    2. **Migration rules** — these are already documented; verify they're still accurate
       and not contradicted by anything new
    3. **Project Structure** — verify the `opencode/` directory listing is accurate
    4. **Running the Project** — verify `pnpm dev`, `pnpm build`, `pnpm db:migrate` commands
    5. **Notes section** — update the sessionRunner placeholder note to reflect that
       Phase 04 implemented actual OpenCode SDK integration (no longer a placeholder)
    6. **Add a "Planning Workflow" section** briefly explaining the state machine,
       skill stack, and how `planPath` is resolved

    Do NOT rewrite AGENTS.md from scratch — make surgical updates only.
  </action>
  <verify>
    `.env.example` has no Turso/libSQL vars.
    `.env.example` has API_TOKEN, PORT (optional), SQLITE_DB_PATH (optional).
    `AGENTS.md` tech stack table is accurate.
    `AGENTS.md` sessionRunner note updated (no longer a placeholder).
  </verify>
  <done>
    `.env.example` is clean and accurate.
    `AGENTS.md` reflects post-refactor project state.
    No stale documentation.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `README.md` fully rewritten, under 200 lines
- [ ] No Turso references anywhere in README or .env.example
- [ ] All commands in README verified against actual package.json scripts
- [ ] `.env.example` contains only API_TOKEN + optional PORT/SQLITE_DB_PATH/MAX_CONCURRENT_SESSIONS
- [ ] `AGENTS.md` tech stack + sessionRunner note updated
- [ ] `AGENTS.md` has planning workflow section
</verification>

<success_criteria>
- All tasks completed
- README is accurate and useful for a fresh clone
- .env.example matches actual env vars used
- AGENTS.md is up-to-date with post-refactor conventions
</success_criteria>

<output>
After completion, create `.planning/pcw-101-ai-dashboard-core-refactor/phases/06-setup-portability/06-01-SUMMARY.md`:

# Phase 06 Plan 01: Docs + Config — Summary

**[Substantive one-liner]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- [Files with descriptions]

## Decisions Made
- [Key decisions]

## Issues Encountered
- [Problems and resolutions, or "None"]

## Next Step
Ready for 06-02-PLAN.md (final build verification + TypeScript cleanup).
</output>
